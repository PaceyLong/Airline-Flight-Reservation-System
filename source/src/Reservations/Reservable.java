package Reservations;

/**
 * Interface which makes a recursive Itinerary-Flight relationship within Reservations to exist
 * (Simple skeleton for potential future utilization of the composite Pattern).
 */
public interface Reservable {
}
